Relative path: avanza-frontend\node_modules\core-js\internals\a-map.js
================================================================================

'use strict';
var has = require('../internals/map-helpers').has;

// Perform ? RequireInternalSlot(M, [[MapData]])
module.exports = function (it) {
  has(it);
  return it;
};
