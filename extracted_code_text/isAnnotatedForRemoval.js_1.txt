Relative path: avanza-frontend\node_modules\babel-plugin-transform-react-remove-prop-types\src\isAnnotatedForRemoval.js
================================================================================

export default function isAnnotatedForRemoval(node) {
  const comments = node.trailingComments || []

  return Boolean(comments.find(({ value }) => value.trim() === 'remove-proptypes'))
}
