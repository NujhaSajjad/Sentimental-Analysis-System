Relative path: avanza-frontend\node_modules\@surma\rollup-plugin-off-main-thread\.travis.yml
================================================================================

sudo: required

services:
  - docker

script:
  - docker build -t omt .
  - docker run --rm -v `pwd`:/usr/src omt
