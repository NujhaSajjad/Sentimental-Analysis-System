Relative path: avanza-frontend\node_modules\core-js\internals\a-weak-map.js
================================================================================

'use strict';
var has = require('../internals/weak-map-helpers').has;

// Perform ? RequireInternalSlot(M, [[WeakMapData]])
module.exports = function (it) {
  has(it);
  return it;
};
